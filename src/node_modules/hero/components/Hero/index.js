import React from "react"
import { Segment, Container, Header, Button, Icon, Dimmer } from "semantic-ui-react"

// CSS
import "semantic-ui-css/semantic.css"
import "./Hero.css"

// Content
import backgroundImage from "hero/content/blurred_code.jpg"

const Hero = () => (
	<div>
		<Dimmer.Dimmable
			as={Segment}
			blurring
			inverted
			dimmed={true}
			style={{
				minHeight: 700,
				padding: "0em 0em",
				borderRadius: 0,
			}}
		>
			<Dimmer active={true}>
				<Container text>
					<Header
						as="h1"
						content="Company Header"
						inverted
						style={{ fontSize: "4em", fontWeight: "normal", marginBottom: 0, marginTop: "0em" }}
					/>
					<Header
						as="h2"
						content="Sub Heading"
						inverted
						style={{ fontSize: "1.7em", fontWeight: "normal" }}
					/>
					<Button primary size="huge">
						A Nice Button
						<Icon name="right arrow" />
					</Button>
				</Container>
			</Dimmer>

			<Segment
				textAlign="center"
				style={{
					minHeight: 700,
					padding: "1em 0em",
					borderRadius: 0,
					background: `url('${backgroundImage}')`,
					backgroundSize: "cover",
				}}
				vertical
			/>
		</Dimmer.Dimmable>
	</div>
)
export default Hero
